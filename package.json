{
  "private": true,
  "workspaces": [
    "./*",
    "apps/*",
    "libs/*",
    "tools/*"
  ],
  "scripts": {
    "del-dist": "lerna exec --parallel -- del-cli dist",
    "prebuild": "yarn del-dist",
    "build": "lerna run build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "del-cli": "^1.1.0",
    "lerna": "^3.13.1"
  },
  "dependencies": {
    "apache-arrow": "^0.4.1",
    "moment": "^2.24.0",
    "node-fetch": "^2.3.0",
    "ts-node": "^8.0.3",
    "typescript": "^3.3.4000",
    "xlsx": "^0.14.1"
  }
}
